- form_for [@product, @sprint, @task] do |f|
  %fieldset
    %legend= t('app.taskks.new')
    = render :partial => 'taskks/form', :locals => { :f => f }
    %p= button_tag t('system.create'), :accept, :class => 'positive'


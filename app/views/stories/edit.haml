= new_link(new_product_story_path(@product), t('app.stories.new'))
- form_for [@product, @story] do |f|
  %fieldset
    %legend= t('app.stories.edit')
    = render :partial => 'stories/form', :locals => { :f => f }
    %p= button_tag t('system.update'), :accept, :class => 'positive', :tabindex => 10


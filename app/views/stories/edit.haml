- form_for([@product, @story]) do |f|
  %fieldset
    %legend= t('app.story.editing')
    = render :partial => 'stories/form', :locals => { :f => f }
    %p= button_tag t('system.update'), :accept, :class => 'positive'

= link_to t('system.back'), product_stories_path(@product)


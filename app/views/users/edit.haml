= new_button(new_user_path, t('app.users.new'))
- form_for @user do |f|
  %fieldset
    %legend= t('app.users.edit')
    = render :partial => 'users/form', :locals => { :f => f }
    %p= button_tag t('system.update'), :accept, :class => 'positive', :tabindex => 10

